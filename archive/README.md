# Archive

This directory stores deprecated or experimental code that was moved out of the main workspace during cleanup.

- `scraper_experiments/`: overlapping local crawler scripts kept only for reference during the rewrite
- `batch_crawler_legacy.py`: the previous batch wrapper that rewrote MediaCrawler config and managed batches externally

Nothing in this directory should be considered the canonical crawler implementation going forward.
